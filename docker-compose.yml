services:
  test:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: pytest -v

  run:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: python run_single_element.py "[1,2,3,4,3,1,2]"